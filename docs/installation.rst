============
Installation
============

At the command line::

    $ easy_install django-sqltables

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sqltables
    $ pip install django-sqltables